from django.urls import path
from django.conf import settings
from django.conf.urls.static import static


import softdesk.views

urlpatterns = [
    # path("allProjects", softdesk.views.ProjectsAPIView.as_view(), name="projects"),
    # path("project/<slug:pk>/contributors", softdesk.views., )
]